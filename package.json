{
  "name": "hankfoot-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "eleventy --serve --port=8090",
    "build": "eleventy",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d _site -t true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gh-pages": "^6.3.0"
  }
}
