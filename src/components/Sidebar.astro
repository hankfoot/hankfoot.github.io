---
const formerly = [
  'meta reality labs research',
  'publicis sapient',
  'spellbound ar',
];
---
<aside class="sidebar">
  <div class="sidebar-top">
    <p class="sidebar-name">hank duhaime <span class="sidebar-hand">👋</span></p>
    <p class="sidebar-bio">
      a <span>programmer</span> turned<br />
      <em>interaction designer</em> —<br />
      specializing in playful,<br />
      hands-on experiences
    </p>

    <div class="formerly">
      <p class="formerly-label">formerly @</p>
      <ul class="formerly-list">
        {formerly.map(co => <li>{co}</li>)}
      </ul>
    </div>
  </div>

  <nav class="sidebar-nav">
    <a href="/">work 👉</a>
    <a href={`https://hankduhaime.com/s/DuhaimeResume2023-V-Nov.pdf`} target="_blank" rel="noopener">resume</a>
    <a href="/#about">about</a>
  </nav>

  <footer class="sidebar-footer">
    <p>seattle, wa</p>
    <a href="mailto:hello.hank.d@gmail.com">hello.hank.d@gmail.com</a>
    <br />
    <a href="https://www.linkedin.com/in/henryduhaime/" target="_blank" rel="noopener">
      linkedin.com/in/henryduhaime/
    </a>

    <button
      class="motion-toggle"
      id="motion-toggle"
      aria-label="Toggle reduced motion"
      data-on="false"
    >
      <span class="motion-icon">✕</span>
      <span class="motion-toggle-track"></span>
      <span class="motion-emoji">👋</span>
    </button>
  </footer>
</aside>

<script>
  function initToggle() {
    const btn = document.getElementById('motion-toggle') as HTMLButtonElement | null;
    if (!btn) return;

    function getReduced(): boolean {
      return document.documentElement.getAttribute('data-reduced-motion') === 'true';
    }

    function sync() {
      btn!.setAttribute('data-on', String(!getReduced()));
    }

    btn.addEventListener('click', () => {
      const next = !getReduced();
      document.documentElement.setAttribute('data-reduced-motion', String(next));
      localStorage.setItem('hank-reduced-motion', String(next));
      sync();
    });

    sync();
  }

  // Run on initial load and after each page transition
  document.addEventListener('astro:page-load', initToggle);
  initToggle();
</script>
